Do not execute the items on this list except if prompted to.

[X] Have in the validation the version validation for nodes (to make sure that we keep all the nodes on the latest version seemlessly !)
[ ] Have a workflow positionning engine (dagre ? how to position notes as well correctly and determine their size ?)
[ ] Make sure a new instance of the n8ncli has all the defaults it needs. Create a workflow developemnt process in the skill (pulling / diff to make sure there are no new updates => workflow specs with a note or something => Gathering usefull examples from the resources => Creating the workflow => Configuring the credentials => Testing the workflow => Pushing & resolving issues / conflicts)